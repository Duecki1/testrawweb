services:
  raw-manager:
    build: .
    ports:
      - "1234:1234"
    volumes:
      - ./data:/data
      # Set RAW_LIBRARY_PATH to your photo library path.
      - ${RAW_LIBRARY_PATH:-/mnt/hdd/raws}:/library:ro
    user: "1000:1000"  # Use host user ID to avoid permission issues
