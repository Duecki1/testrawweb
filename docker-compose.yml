version: "3.9"
services:
  raw-manager:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
      # Set RAW_LIBRARY_PATH to your photo library path.
      - ${RAW_LIBRARY_PATH:-./photos}:/library:ro
