services:
  raw-manager:
    build: .
    ports:
      - "1234:1234"
    volumes:
      - ./data:/data
      - /mnt/hdd/raws:/library:ro
    user: "1000:1000"  # Use host user ID to avoid permission issues
